"""Walk-forward backtest runner.

P5.1 — runs `decide()` daily over a date range, marks to next-day actuals.
Status: skeleton. Implement after Phase 4 lands.
"""

from datetime import date


def run_backtest(country: str, start: date, end: date) -> str:
    raise NotImplementedError(
        "P5.1 not yet implemented. Decision agent must exist first."
    )
